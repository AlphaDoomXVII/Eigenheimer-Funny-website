# Kopieer dit bestand naar ".env" (staat in .gitignore, wordt dus nooit gecommit) en vul
# de echte waarden in. Ontbreekt .env, dan gelden de defaults in config/config.php.

DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=eigenheimer
DB_USERNAME=timo
DB_PASSWORD=timo
